{
  "name": "ravikishan-platform",
  "version": "1.0.0",
  "private": true,
  "description": "NEB +2 educational platform with tiered access control, AI chat, and interactive labs",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "start": "npm run start --workspace=frontend",
    "lint": "npm run lint --workspace=frontend",
    "test": "npm run test --workspace=frontend",
    "test:e2e": "npm run test:e2e --workspace=frontend",
    "seed:notes": "npm run seed:notes --workspace=frontend",
    "migrate:syllabus": "npm run migrate:syllabus --workspace=frontend"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
